version: "3"

services:
  web:
    build:
      context: .
      dockerfile: ./node/Dockerfile
    image: jblog-client_image
    container_name: 'jblog-client_container'
    environment:
    - NODE_ENV=production
    ports:
    - 8081:8081
    restart: always



